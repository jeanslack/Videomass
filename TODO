
--------------------
URGENCY: to do soon
--------------------
- Preview video stabilization

--------------
URGENCY: high
--------------

- FFmpeg: for the new versions -vsync arg is deprecated, use fps_mode
  (video_to_sequences.py, concatenete.py). For backwards compatibility reasons
  I will keep the -vsync argument for a while longer.

---------------
URGENCY: medium
----------------
- If you plan to refactor code and make it compatible only for
  wx.Python4.1.1/wxPython4.2.0:
  - erase `TestListCtrl` class on youtubedl_ui.py. Also check all code that
    starts with wx.version().
  - erase
        try:
            from wx.svg import SVGimage
        except ModuleNotFoundError:
            pass

  However, note that these operations leads to make Videomass code incompatible
  for wxPython4.0.7.

-------------
URGENCY: low
-------------
